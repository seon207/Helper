<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">
<Button
    android:id="@+id/btn_environ"
    android:layout_width="match_parent"
    android:layout_height="80dp"
    android:text="환경 정보"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    android:backgroundTint="#8BB1FD"
    android:textSize="10pt"
    />

    <Button
        android:id="@+id/write"
        android:layout_width="80dp"
        android:layout_height="40dp"
        android:layout_marginTop="45dp"
        android:backgroundTint="#CCCCCC"
        android:text="글쓰기"
        android:textColor="@color/black"
        android:textSize="5pt"
        app:layout_constraintEnd_toEndOf="@id/btn_environ"
        app:layout_constraintHorizontal_bias="0.965"
        app:layout_constraintStart_toStartOf="@id/btn_environ"
        app:layout_constraintTop_toBottomOf="@id/btn_environ" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="850dp"
        android:layout_marginTop="20dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/write" />
</androidx.constraintlayout.widget.ConstraintLayout>